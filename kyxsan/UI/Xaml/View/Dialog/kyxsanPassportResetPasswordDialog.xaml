<ContentDialog
    x:Class="kyxsan.UI.Xaml.View.Dialog.kyxsanPassportResetPasswordDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shuxm="using:kyxsan.UI.Xaml.Markup"
    Title="{shuxm:ResourceString Name=ViewDialogkyxsanPassportResetPasswordTitle}"
    CloseButtonText="{shuxm:ResourceString Name=ContentDialogCancelCloseButtonText}"
    DefaultButton="Primary"
    PrimaryButtonText="{shuxm:ResourceString Name=ContentDialogConfirmPrimaryButtonText}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <StackPanel Margin="0,8,0,0" Spacing="16">
        <TextBox
            x:Name="UserNameTextBox"
            InputScope="EmailSmtpAddress"
            IsSpellCheckEnabled="False"
            PlaceholderText="{shuxm:ResourceString Name=ViewPagekyxsanPassportUserNameHint}"
            Text="{x:Bind UserName, Mode=TwoWay}"/>
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                PlaceholderText="{shuxm:ResourceString Name=ViewPagekyxsanPassportVerifyCodeHint}"
                Text="{x:Bind VerifyCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button
                Grid.Column="1"
                VerticalAlignment="Stretch"
                Command="{x:Bind VerifyCommand}"
                Content="{shuxm:ResourceString Name=ViewPagekyxsanPassportVerifyCodeAction}"/>
        </Grid>
        <StackPanel Spacing="4" Visibility="{x:Bind VerifyCode, Converter={StaticResource EmptyStringToVisibilityConverter}, Mode=OneWay}">
            <PasswordBox
                IsEnabled="{x:Bind VerifyCode, Converter={StaticResource EmptyStringToBoolConverter}, Mode=OneWay}"
                Password="{x:Bind Password, Mode=TwoWay}"
                PasswordChar="*"
                PlaceholderText="{shuxm:ResourceString Name=ViewPagekyxsanPassportPasswordHint}"/>
            <TextBlock
                Margin="0,4,0,0"
                Opacity="0.7"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{shuxm:ResourceString Name=ViewPagekyxsanPassportPasswordRequirementHint}"/>
        </StackPanel>
    </StackPanel>
</ContentDialog>