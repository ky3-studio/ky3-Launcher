<ContentDialog
    x:Class="kyxsan.UI.Xaml.View.Dialog.LaunchGamePackageConvertDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shuxm="using:kyxsan.UI.Xaml.Markup"
    xmlns:shuxvd="using:kyxsan.UI.Xaml.View.Dialog"
    d:DataContext="{d:DesignInstance shuxvd:LaunchGamePackageConvertDialog}"
    Style="{StaticResource DefaultContentDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Title>
        <StackPanel Orientation="Horizontal" Spacing="8">
            <ProgressRing IsActive="True" Width="20" Height="20"/>
            <TextBlock Text="{shuxm:ResourceString Name=ViewDialogLaunchGamePackageConvertTitle}" VerticalAlignment="Center"/>
        </StackPanel>
    </ContentDialog.Title>

    <StackPanel>
        <TextBlock Text="{shuxm:ResourceString Name=ViewDialogLaunchGamePackageConvertHint}"/>
        <ProgressBar
            Margin="0,16,0,4"
            IsIndeterminate="{Binding State.IsIndeterminate}"
            Maximum="1"
            Value="{Binding State.Percent}"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{Binding State.Name}"
                TextWrapping="NoWrap"/>
            <TextBlock
                Grid.Column="1"
                Margin="16,0,0,0"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{Binding State.Description}"/>
        </Grid>
    </StackPanel>
</ContentDialog>
