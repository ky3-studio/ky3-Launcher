<shuxc:ScopedPage
    x:Class="kyxsan.UI.Xaml.View.Page.WikiArchonQuestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cw="using:CommunityToolkit.WinUI"
    xmlns:cwc="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mxi="using:Microsoft.Xaml.Interactivity"
    xmlns:shmmq="using:kyxsan.Model.Metadata.Quest"
    xmlns:shuxb="using:kyxsan.UI.Xaml.Behavior"
    xmlns:shuxc="using:kyxsan.UI.Xaml.Control"
    xmlns:shuxca="using:kyxsan.UI.Xaml.Control.AutoSuggestBox"
    xmlns:shuxcc="using:kyxsan.UI.Xaml.Control.Card"
    xmlns:shuxci="using:kyxsan.UI.Xaml.Control.Image"
    xmlns:shuxm="using:kyxsan.UI.Xaml.Markup"
    xmlns:shvw="using:kyxsan.ViewModel.Wiki"
    d:DataContext="{d:DesignInstance Type=shvw:WikiArchonQuestViewModel}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">
    <mxi:Interaction.Behaviors>
        <shuxb:InvokeCommandOnLoadedBehavior Command="{Binding LoadCommand}"/>
    </mxi:Interaction.Behaviors>

    <Page.Resources>
        <DataTemplate x:Key="TokenTemplate" x:DataType="shuxca:SearchToken">
            <Grid Height="22" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <shuxci:CachedImage
                    Grid.Column="0"
                    Width="20"
                    Height="20"
                    Margin="-5,2,9,0"
                    ShowAsMonoChrome="True"
                    Source="{Binding IconUri, Mode=OneWay}"
                    Visibility="{Binding IconUri, Converter={StaticResource EmptyObjectToVisibilityConverter}}"/>
                <shuxci:CachedImage
                    Grid.Column="0"
                    Width="20"
                    Height="20"
                    Margin="-4,0,8,0"
                    Source="{Binding SideIconUri, Mode=OneWay}"
                    Visibility="{Binding SideIconUri, Converter={StaticResource EmptyObjectToVisibilityConverter}}"/>
                <TextBlock
                    Grid.Column="1"
                    Height="20"
                    VerticalAlignment="Center"
                    Text="{Binding Value}"/>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="QuestListTemplate" x:DataType="shmmq:ArchonQuest">
            <Grid Padding="4,8" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <shuxci:CachedImage
                    Grid.Column="0"
                    Width="40"
                    Height="40"
                    Source="{x:Bind ChapterIcon, Converter={StaticResource ChapterIconConverter}}"/>
                <StackPanel
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Spacing="2">
                    <TextBlock Text="{x:Bind ChapterNum}" TextTrimming="CharacterEllipsis"/>
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text="{x:Bind ChapterTitle}"/>
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="QuestGridTemplate" x:DataType="shmmq:ArchonQuest">
            <shuxcc:VerticalCard Width="120">
                <shuxcc:VerticalCard.Top>
                    <shuxci:CachedImage
                        Width="80"
                        Height="80"
                        Source="{x:Bind ChapterIcon, Converter={StaticResource ChapterIconConverter}}"/>
                </shuxcc:VerticalCard.Top>
                <shuxcc:VerticalCard.Bottom>
                    <TextBlock
                        Margin="4,0"
                        HorizontalAlignment="Center"
                        Text="{x:Bind ChapterTitle}"
                        TextWrapping="Wrap"
                        MaxLines="2"
                        TextAlignment="Center"/>
                </shuxcc:VerticalCard.Bottom>
            </shuxcc:VerticalCard>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Border Background="#40000000"/>
        <shuxc:StandardView ShowCondition="{Binding Quests, Converter={StaticResource EmptyObjectToBoolConverter}}">
            <shuxc:StandardView.EmptyContent>
                <shuxc:Loading IsLoading="{Binding Quests, Converter={StaticResource EmptyObjectToBoolRevertConverter}}" Style="{ThemeResource DefaultLoadingViewStyle}"/>
            </shuxc:StandardView.EmptyContent>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Border Margin="16" cw:Effects.Shadow="{ThemeResource CompatCardShadow}">
                    <Border Style="{ThemeResource AcrylicSecondaryBorderCardStyle}">
                        <CommandBar
                            BorderBrush="{x:Null}"
                            BorderThickness="0"
                            DefaultLabelPosition="Right">
                            <CommandBar.Content>
                                <StackPanel Orientation="Horizontal" Spacing="12">
                                    <shuxc:LayoutSwitch
                                        x:Name="ItemsPanelSelector"
                                        Margin="6,8,0,0"
                                        LocalSettingKeySuffixForCurrent="WikiArchonQuestPage.Quests"/>
                                    <StackPanel
                                        Margin="0,6,0,0"
                                        Orientation="Horizontal"
                                        Spacing="16"
                                        Visibility="{Binding Quests, Converter={StaticResource EmptyObjectToVisibilityConverter}}">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <shuxci:CachedImage
                                                Width="20"
                                                Height="20"
                                                Source="{Binding AdventureExpIconUri}"/>
                                            <TextBlock
                                                VerticalAlignment="Center"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Text="{Binding TotalAdventureExp}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <shuxci:CachedImage
                                                Width="20"
                                                Height="20"
                                                Source="{Binding PrimogemIconUri}"/>
                                            <TextBlock
                                                VerticalAlignment="Center"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Text="{Binding TotalPrimogem}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <shuxci:CachedImage
                                                Width="20"
                                                Height="20"
                                                Source="{Binding MoraIconUri}"/>
                                            <TextBlock
                                                VerticalAlignment="Center"
                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                Text="{Binding TotalMora}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </CommandBar.Content>
                            <AppBarElementContainer>
                                <shuxca:AutoSuggestTokenBox
                                    x:Name="QuestSuggestBox"
                                    Width="600"
                                    Height="44"
                                    Margin="6,-2,6,6"
                                    HorizontalAlignment="Stretch"
                                    VerticalContentAlignment="Center"
                                    AvailableTokens="{Binding SearchData.AvailableTokens}"
                                    FilterCommand="{Binding FilterCommand}"
                                    ItemsSource="{Binding SearchData.FilterTokens, Mode=TwoWay}"
                                    MaximumTokens="5"
                                    PlaceholderText="{shuxm:ResourceString Name=ViewPageWikiArchonQuestAutoSuggestBoxPlaceHolder}"
                                    QueryIcon="{cw:FontIconSource Glyph=&#xE721;}"
                                    Style="{StaticResource DefaultAutoSuggestTokenBoxStyle}"
                                    SuggestedItemTemplate="{StaticResource TokenTemplate}"
                                    SuggestedItemsSource="{Binding SearchData.AvailableTokens.Values}"
                                    Text="{Binding SearchData.FilterToken, Mode=TwoWay}"
                                    TokenItemTemplate="{StaticResource TokenTemplate}"/>
                            </AppBarElementContainer>
                        </CommandBar>
                    </Border>
                </Border>

                <cwc:SwitchPresenter
                    Grid.Row="1"
                    ContentTransitions="{ThemeResource EntranceThemeTransitions}"
                    Value="{Binding ElementName=ItemsPanelSelector, Path=Current}">
                    <cwc:Case Value="List">
                        <Border Margin="16,0,16,16" cw:Effects.Shadow="{ThemeResource CompatCardShadow}">
                            <Border Style="{ThemeResource AcrylicBorderCardStyle}">
                                <SplitView
                                    DisplayMode="Inline"
                                    IsPaneOpen="True"
                                    OpenPaneLength="{StaticResource CompatSplitViewOpenPaneLength}"
                                    PaneBackground="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                    <SplitView.Pane>
                                        <ListView
                                            Padding="{ThemeResource ListViewInSplitPanePadding}"
                                            ItemTemplate="{StaticResource QuestListTemplate}"
                                            ItemsSource="{Binding Quests}"
                                            SelectedItem="{Binding Quests.CurrentItem, Mode=TwoWay}"
                                            SelectionMode="Single">
                                            <mxi:Interaction.Behaviors>
                                                <shuxb:SelectedItemInViewBehavior/>
                                            </mxi:Interaction.Behaviors>
                                        </ListView>
                                    </SplitView.Pane>
                                    <SplitView.Content>
                                        <Grid>
                                            <StackPanel VerticalAlignment="Center" Visibility="{Binding Quests.CurrentItem, Converter={StaticResource EmptyObjectToVisibilityRevertConverter}}">
                                                <shuxci:CachedImage
                                                    Height="120"
                                                    MinWidth="{ThemeResource SettingsCardContentControlMinWidth}"
                                                    Source="{StaticResource UI_EmotionIcon89}"/>
                                                <TextBlock
                                                    Margin="0,5,0,21"
                                                    HorizontalAlignment="Center"
                                                    Style="{StaticResource SubtitleTextBlockStyle}"
                                                    Text="{shuxm:ResourceString Name=ControlAutoSuggestBoxNotFoundValue}"/>
                                            </StackPanel>
                                            <ScrollViewer shuxc:ScrollViewerHelper.ScrollToTopAssociatedObject="{Binding Quests.CurrentItem}" Visibility="{Binding Quests.CurrentItem, Converter={StaticResource EmptyObjectToVisibilityConverter}}">
                                                <StackPanel Padding="16" Spacing="16">
                                                    <!--  Header: Chapter Info  -->
                                                    <Border Padding="16" Style="{ThemeResource BorderCardStyle}">
                                                        <Grid ColumnSpacing="16">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition/>
                                                            </Grid.ColumnDefinitions>
                                                            <shuxci:CachedImage
                                                                Grid.Column="0"
                                                                Width="72"
                                                                Height="72"
                                                                Source="{Binding Quests.CurrentItem.ChapterIcon, Converter={StaticResource ChapterIconConverter}}"/>
                                                            <StackPanel
                                                                Grid.Column="1"
                                                                VerticalAlignment="Center"
                                                                Spacing="4">
                                                                <TextBlock
                                                                    Style="{StaticResource SubtitleTextBlockStyle}"
                                                                    Text="{Binding Quests.CurrentItem.ChapterNum}"/>
                                                                <TextBlock
                                                                    Opacity="0.7"
                                                                    Text="{Binding Quests.CurrentItem.ChapterTitle}"/>
                                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                                    <Border
                                                                        Padding="8,4"
                                                                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                                        CornerRadius="4">
                                                                        <TextBlock
                                                                            Style="{StaticResource CaptionTextBlockStyle}"
                                                                            Text="{Binding Quests.CurrentItem.Region}"/>
                                                                    </Border>
                                                                </StackPanel>
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>

                                                    <!--  Stories List  -->
                                                    <Border Padding="16" Style="{ThemeResource BorderShimmerStyle}">
                                                        <StackPanel Spacing="12">
                                                            <TextBlock Style="{StaticResource BaseTextBlockStyle}" Text="任务列表"/>
                                                            <ItemsControl ItemsSource="{Binding Quests.CurrentItem.Stories}">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate x:DataType="shmmq:ArchonQuestStory">
                                                                        <Grid
                                                                            Margin="0,0,0,8"
                                                                            Padding="12,10"
                                                                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                                            ColumnSpacing="12"
                                                                            CornerRadius="4">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                                <ColumnDefinition/>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                            </Grid.ColumnDefinitions>

                                                                            <TextBlock
                                                                                Grid.Column="0"
                                                                                MinWidth="24"
                                                                                VerticalAlignment="Center"
                                                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                                                Text="{x:Bind Id}"
                                                                                TextAlignment="Center"/>

                                                                            <TextBlock
                                                                                Grid.Column="1"
                                                                                VerticalAlignment="Center"
                                                                                Text="{x:Bind Title}"/>

                                                                            <ItemsRepeater
                                                                                Grid.Column="2"
                                                                                VerticalAlignment="Center"
                                                                                ItemsSource="{x:Bind Rewards}">
                                                                                <ItemsRepeater.Layout>
                                                                                    <StackLayout Orientation="Horizontal" Spacing="4"/>
                                                                                </ItemsRepeater.Layout>
                                                                                <ItemsRepeater.ItemTemplate>
                                                                                    <DataTemplate x:DataType="shmmq:ArchonQuestReward">
                                                                                        <StackPanel Orientation="Horizontal" Spacing="2">
                                                                                            <shuxci:CachedImage
                                                                                                Width="20"
                                                                                                Height="20"
                                                                                                Source="{x:Bind Icon, Converter={StaticResource ItemIconConverter}}"/>
                                                                                            <TextBlock
                                                                                                VerticalAlignment="Center"
                                                                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                                                Style="{StaticResource CaptionTextBlockStyle}"
                                                                                                Text="{x:Bind Count}"/>
                                                                                        </StackPanel>
                                                                                    </DataTemplate>
                                                                                </ItemsRepeater.ItemTemplate>
                                                                            </ItemsRepeater>
                                                                        </Grid>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </StackPanel>
                                                    </Border>
                                                </StackPanel>
                                            </ScrollViewer>
                                        </Grid>
                                    </SplitView.Content>
                                </SplitView>
                            </Border>
                        </Border>
                    </cwc:Case>
                    <cwc:Case Value="Grid">
                        <Border Margin="16,0,16,16" cw:Effects.Shadow="{ThemeResource CompatCardShadow}">
                            <Border Style="{ThemeResource AcrylicBorderCardStyle}">
                                <Grid Padding="16,16,4,-6">
                                    <StackPanel VerticalAlignment="Center" Visibility="{Binding Quests.Count, Converter={StaticResource Int32ToVisibilityRevertConverter}}">
                                        <shuxci:CachedImage
                                            Height="120"
                                            MinWidth="{ThemeResource SettingsCardContentControlMinWidth}"
                                            Source="{StaticResource UI_EmotionIcon89}"/>
                                        <TextBlock
                                            Margin="0,5,0,21"
                                            HorizontalAlignment="Center"
                                            Style="{StaticResource SubtitleTextBlockStyle}"
                                            Text="{shuxm:ResourceString Name=ControlAutoSuggestBoxNotFoundValue}"/>
                                    </StackPanel>
                                    <GridView
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        ItemContainerStyle="{StaticResource LargeGridViewItemStyle}"
                                        ItemTemplate="{StaticResource QuestGridTemplate}"
                                        ItemsSource="{Binding Quests}"
                                        SelectedItem="{Binding Quests.CurrentItem, Mode=TwoWay}"
                                        SelectionMode="Single"
                                        Visibility="{Binding Quests.Count, Converter={StaticResource Int32ToVisibilityConverter}}">
                                        <mxi:Interaction.Behaviors>
                                            <shuxb:SelectedItemInViewBehavior/>
                                        </mxi:Interaction.Behaviors>
                                    </GridView>
                                </Grid>
                            </Border>
                        </Border>
                    </cwc:Case>
                </cwc:SwitchPresenter>
            </Grid>
        </shuxc:StandardView>
    </Grid>
</shuxc:ScopedPage>
